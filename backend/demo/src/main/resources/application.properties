spring.config.import=optional:file:.env[.properties]

# Google OAuth2 credentials are loaded from your local environment.
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/v2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v3/userinfo

# Local database settings. Keep secrets in env vars, not in git.
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/youtube_clone}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

server.servlet.session.cookie.same-site=lax
server.servlet.session.cookie.secure=false
server.port=8080
spring.graphql.graphiql.enabled=true



Invoke-RestMethod -Uri "http://localhost:8080/api/comments" `
  -Method POST `
  -Headers @{
    "Content-Type" = "application/json"
    "Cookie" = "JSESSIONID=3F94EE921A0714F235FDC3B03D86CE20"
    "Idempotency-Key" = "test-123"
  } `
  -Body '{ "videoId": "1", "text": "idempotency test", "parentCommentId": null }'

Invoke-RestMethod -Uri "http://localhost:8080/api/comments" `
  -Method POST `
  -Headers @{
    "Content-Type" = "application/json"
    "Cookie" = "JSESSIONID=3F94EE921A0714F235FDC3B03D86CE20"
    "Idempotency-Key" = "test-123"
  } `
  -Body '{ "videoId": "1", "text": "idempotency test", "parentCommentId": null }'
